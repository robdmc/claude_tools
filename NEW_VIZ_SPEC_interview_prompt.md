# Interview Prompt — NEW_VIZ_SPEC.md

You are creating a specification document for **a modified version of the `presentation` skill that replaces the current Marp-based pipeline with the `document-skills:pptx` skill (html2pptx workflow)**. The user wants to replace Marp entirely and prioritizes simplicity over design control.

Interview me in detail using AskUserQuestion about literally anything. But make sure the questions are not obvious. Be very in-depth and continue interviewing me continually until it's complete, then write the spec to the file.

Angles you may want to consider:
- Sketch format compatibility (which current components survive unchanged vs. need remapping)
- Parallel agent strategy (how agents contribute when output is binary, not text)
- Theming and styling (what replaces the current Marp CSS template)
- Removed infrastructure (what files/scripts become obsolete)
